# docker-compose.yml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules # コンテナ内の node_modules を上書きされないように保護
    environment:
      - CHOKIDAR_USEPOLLING=true # Windowsや一部環境でホットリロードを確実にする設定